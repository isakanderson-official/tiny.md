{
  "name": "tiny.md",
  "description": "A tiny folder-and-Markdown static site generator.",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch --serve"
  }
}
